# Core agent framework
langgraph>=0.2.50
langchain-core>=0.3.0

# LLM providers (only the one you use needs an API key)
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
# Local/free option (optional, install separately if you use Ollama):
# langchain-ollama>=0.2.0

# Structured outputs & config
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# Web search tools
tavily-python>=0.5.0   # used if TAVILY_API_KEY is set
ddgs>=6.0.0            # no-key DuckDuckGo fallback

# UI
streamlit>=1.36.0

# Resume parsing (PDF support)
pypdf>=4.2.0
