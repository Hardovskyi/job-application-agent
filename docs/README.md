# Screenshots

Drop demo images here so they render in the main README:

- `dashboard.png` — the Streamlit app after a run (match score + tailored output)
- `langsmith-trace.png` — a run opened in smith.langchain.com showing the agent
  tree, the `web_search` tool call, and the review-loop iterations

A short screen-recording (`demo.gif`) is also a great addition.
